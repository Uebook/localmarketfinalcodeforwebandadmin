// Global sidebar control for all screens
let sidebarControl = null;

export const setSidebarControl = (control) => {
  sidebarControl = control;
};

export const getSidebarControl = () => sidebarControl;


